<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= review.business_name %> - Review Details</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 900px;
            margin: 0 auto;
        }
        .header {
            background: white;
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .btn {
            padding: 10px 18px;
            border-radius: 10px;
            background: #6c757d;
            color: white;
            border: none;
            font-size: 14px;
            cursor: pointer;
            text-decoration: none;
            font-weight: bold;
        }
        .btn:hover {
            background: #545b62;
        }
        .card {
            background: white;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .review-header {
            margin-bottom: 20px;
        }
        .business-name {
            font-size: 2rem;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 5px;
        }
        .meta {
            color: #7f8c8d;
            font-size: 0.95rem;
        }
        .stars {
            color: #ffc107;
            font-weight: bold;
            margin-top: 10px;
        }
        .image-wrapper {
            margin: 25px 0;
        }
        .image-wrapper img {
            width: 100%;
            max-height: 350px;
            object-fit: cover;
            border-radius: 12px;
        }
        .review-text {
            font-size: 1rem;
            line-height: 1.7;
            color: #34495e;
            margin-bottom: 25px;
        }
        .byline {
            color: #7f8c8d;
            font-size: 0.95rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Review Details</h1>
            <div>
                <a href="/browse" class="btn">← Back to Browse</a>
                <a href="/dashboard" class="btn" style="margin-left: 10px;">Dashboard</a>
            </div>
        </div>

        <div class="card">
            <% 
                const imgUrl =
                    review.image_url ||
                    (review.photos && review.photos.length > 0 && review.photos[0].photo_url) ||
                    'https://via.placeholder.com/800x350?text=No+Image';
            %>

            <div class="review-header">
                <div class="business-name"><%= review.business_name %></div>
                <div class="meta">
                    <% if (review.location && review.location.city) { %>
                        <%= review.location.city %>
                    <% } %>
                    <% if (review.categories && review.categories.length) { %>
                        • <%= review.categories.join(', ') %>
                    <% } %>
                    • Reviewed on <%= new Date(review.review_date).toLocaleDateString() %>
                </div>
                <div class="stars">
                    <% for (let i = 0; i < review.review_stars; i++) { %>★<% } %>
                    <% for (let i = review.review_stars; i < 5; i++) { %>☆<% } %>
                    <%= review.review_stars %>/5
                </div>
            </div>

            <div class="image-wrapper">
                <img src="<%= imgUrl %>" alt="<%= review.business_name %>">
            </div>

            <div class="review-text">
                <%= review.review_text %>
            </div>

            <div class="byline">
                By <strong><%= review.user_name %></strong>
            </div>
        </div>
    </div>
</body>
</html>
